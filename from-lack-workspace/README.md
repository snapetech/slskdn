# Artifacts from lack workspace

This folder holds **slskdn-related plans and docs** that were created or collected in the lack project context. They are kept here for reference only—**do not merge** them into the main slskdn tree unless you are explicitly implementing that work.

## Contents

| File / folder | Purpose |
|---------------|---------|
| `slskdn_code_review.md` | Code review notes |
| `slskdn_fix_plan_enforce.md` | Fix plan for enforce-related work |
| `slskdn_mesh_hybrid_search_plan.md` | Plan for mesh/hybrid search |
| `slskdn-docs/docs/` | Spec-style docs (issue linking, provider UX, identity, adapters, policy schema, security) |

## For agents

When working on slskdn and the task relates to code review, enforce fixes, mesh/hybrid search, or the topics in `slskdn-docs/docs/`, read the relevant file here first. Use them as reference; do not merge or copy content into the main repo unless you are implementing that work.
